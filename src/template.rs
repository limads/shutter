use opencv::{core, imgproc};
use crate::image::cvutils::slice_to_mat;

/*pub fn template_match(a : &mut [T], b : &mut [U]) {

    // Return match values
    imgproc::match_template(
        image: &dyn ToInputArray, 
        templ: &dyn ToInputArray, 
        result: &mut dyn ToOutputArray, 
        imgproc::TM_CCORR_NORMED, 
        core::no_array().unwrap()
    ) -> Result<()>
    
    // Then find local maxima and minima
    pub fn min_max_loc(
        src: &dyn ToInputArray, 
        min_val: &mut f64, 
        max_val: &mut f64, 
        min_loc: &mut Point, 
        max_loc: &mut Point, 
        core::no_array().unwrap()
    ) -> Result<()>
}
}*/


